<%
err = []
begin
    # delete the action
    DB.execute("UPDATE eml_action SET status=false WHERE id='#{params[:aid]}'")
    # return
    redirect "/emails/actions?msg="+ CGI::escape('Action paused.')
rescue => e
    # return errors
    redirect "/emails/actions?err="+ CGI::escape(e.message)
end
%>