<%
begin
	raise "Cannot change the owner of a subaccount." if SLAVE
	@login.user.set_account_owner(params)
rescue => e
	redirect "/settings/users?err="+CGI::escape(e.message)
end
redirect "/settings/users?msg=#{CGI::escape("New User Assigned as the Account Owner.")}"
%>